{$MEETIFIER_DOMAIN:localhost} {
    encode zstd gzip

    @meetifier_endpoints path /healthz /oauth/google/callback /oauth/google/success
    handle @meetifier_endpoints {
        reverse_proxy app:8080
    }

    respond "Not found" 404
}
